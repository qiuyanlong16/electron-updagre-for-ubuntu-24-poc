case-13: LATEST — runtime-confirmed (2026-08-28).
After the 1.1.0->1.2.0 upgrade + operator restart, the relaunched byclaw app is at 1.2.0,
which equals the latest published version (policy latestVersion=1.2.0 at that time), so the
state machine resolves to LATEST and the UI shows "已是最新版本" (already latest version).
Evidence:
  - screenshot: evidence-v2/case-13-latest.png (captured 12:52, 259 KB; rgb variant alongside).
  - operator visual: the operator confirmed the app displayed "already latest version" (the
    user's own message after restart: "if show already latest version").
  - runtime corroboration: dpkg.log 'configure byclaw:amd64 1.2.0' (12:47:25) +
    'status installed byclaw:amd64 1.2.0' (12:47:25); the served policy latestVersion was 1.2.0
    at capture time (1.3.0 was not published until 13:21), so running 1.2.0 == latest 1.2.0 => LATEST.
Note: an earlier run of case-13.sh wrote a "deferred — app not installed" line here (before the
root install). That note is superseded by the real capture above; the screenshot is genuine.
The controller cannot render PNG text (harness limit), so operator visual confirmation is
recorded — not a code-review claim.
Verdict: PASS.
